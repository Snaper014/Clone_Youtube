# Ignorer les fichiers d'environnement
.env

# Ignorer les modules Node.js
node_modules

# Ignorer les fichiers spécifiques à npm
package-lock.json

# Ignorer le dossier de production Vercel
/.vercel

# Ignorer le dossier de production (s'il est généré)
/build

# Ignorer les fichiers de configuration spécifiques à l'environnement (s'ils sont générés)
.env.local
.env.development.local
.env.test.local
.env.production.local

